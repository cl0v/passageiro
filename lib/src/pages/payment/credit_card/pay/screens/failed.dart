import 'package:flutter/material.dart';

class CreditCardPaymentFailedScreen extends StatelessWidget {
  const CreditCardPaymentFailedScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}